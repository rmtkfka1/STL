
1.https://namu.wiki/w/C%2B%2B 읽어보기

c++  ->
object-oriented 
->Desing pattern

Generics
->자료형과 무관하게 코딩

연산자 오버로딩 공부하기

============================================
오버로딩이 이 가능한이유:  name mangling
	

	cout << *max_element(dogs.begin(), dogs.end(), 누가 더큰건지 판단하는 함수) << endl;

	end - begine = 800 바이트

//스택은 메모리구조가 낮은 주소에서 부터 높은 주소로 쌓임







// sort 로 100개 정렬 = >  100*log2(100) , 즉 600~700번 정렬함수 호출


#include <ragnes> => views::drop()


===============================================================================================================
4월 16일

데이터무결성 => 벡터는 이사를 갈때 이동생성자를 사용하지않고 복사를 해서 이사를감.

But noexcept 를 붙어주면 이동생성자를 이용함.